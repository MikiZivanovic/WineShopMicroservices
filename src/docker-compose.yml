version: '3.4'

services:
  catalogdb:
    image: postgres

  cartdb:
    image: postgres

  catalogapi:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/CatalogAPI/Dockerfile

volumes:
  postgres_catalog:
  postgres_cart: